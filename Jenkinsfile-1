pipeline {
    agent any
     tools {
        nodejs 'NODE_HOME'
    }

     stages {
        
                stage('git') {
            steps {
            
            git 'https://github.com/eyadrihmi/jenkins-example-ansible.git'
            }}
            
    stage('Hello') {
      steps {
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
